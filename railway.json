{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "sh -c 'cd /app && pwd && ls -la && if [ -f package.json ]; then npm start; elif [ -f server/index.js ]; then node server/index.js; else echo \"ERROR: Cannot find package.json or server/index.js\" && exit 1; fi'",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}

